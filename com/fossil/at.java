package com.fossil;

import android.app.Dialog;
import android.os.Bundle;

public class at extends pd {
    public Dialog onCreateDialog(Bundle bundle) {
        return new as(getContext(), getTheme());
    }
}
