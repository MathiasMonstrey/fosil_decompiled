package com.fossil;

import java.util.Locale;

public class tj {
    private final tl axs;

    public boolean tb() {
        return this.axs.tb();
    }

    public String toString() {
        return String.format(Locale.US, "%s@%s[cancellationRequested=%s]", new Object[]{getClass().getName(), Integer.toHexString(hashCode()), Boolean.toString(this.axs.tb())});
    }
}
